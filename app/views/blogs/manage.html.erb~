<table class="listing">
  <thead>
    <tr>
      <th>Name</th>
      <th width="30"></th>
     <tr>  
  </thead>
  <tbody>
    <% for blog in @blogs %>
      <tr>
        <td><%= link_to blog.name, blog_path(blog) %></td>
        <td><%= link_to image_tag('/images/edit_10.png', :border => "0"),edit_blog_path(blog) %>
       <% if  %>
        <%=link_to image_tag('/images/delete_10.png', :border => "0"),blog_path(blog),:confirm => "Are u sure u want to delete '#{blog.name}' blog?",:method => :delete, :title => "Delete #{blog.name}"%>
        <% end %>
        </td>
       </tr>
    <% end %>
  </tbody>
</table>    
